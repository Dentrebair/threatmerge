---
status: accepted
---

# Version domain and tenant rules

The internally maintained Real Estate Domain Schema Pack supplies bounded defaults, while tenants configure versioned schemas, templates, Approval Policies, and Conflict Precedence Policies. Every resolved record stores the exact rule versions or Effective Invoice Schema fingerprint used so later policy changes cannot silently rewrite historical decisions.

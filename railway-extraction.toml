[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run service:extraction-adapter"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
